/**
 * RevenueCat configuration for ShapeBeats.
 * Make sure these values match your RevenueCat dashboard.
 */

/**
 * Entitlement identifier — must match the RevenueCat dashboard exactly
 * (including spaces/casing).
 *
 * Example in RevenueCat:
 *   Entitlement Identifier: "Lifetime Access"
 */
export const ENTITLEMENT_LIFETIME = 'Shape Beats Full Unlock';

/**
 * Product identifier for the one‑time lifetime purchase.
 * This must match the product ID in App Store Connect / Google Play.
 *
 * Example:
 *   com.yourcompany.shapebeats.lifetime
 */
export const PRODUCT_ID_LIFETIME = 'shapebeats_unlock';
